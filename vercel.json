{
  "version": 2,
  "builds": [
    {
      "src": "API/bot.js", // Шлях до вашого коду
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/webhook", // Шлях, який використовує Telegram
      "dest": "/API/bot.js" // Шлях до файлу коду
    }
  ]
}
